# Variables and Attributes:
MIN_LIFESPAN
MAX_LIFESPAN
self.age
self.lifespan

# Function:
1. A is_dead function that returns self.age >= self.lifespan.
2. A function that destroys the square and creates a random square if is_dead == true
